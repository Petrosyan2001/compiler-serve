export enum Comands {
    Ts = "sudo apt-get install -y task-spooler",
    Node = 'node',
    KillTs = 'tsp -K',
    KillFinished = 'tsp -C',
    Dir = 'tmp',
    'DirC++' = 'cplus',
    "C++" = 'gcc',
    "C++Install" = 'apt-get install build-essential manpages-dev',
    C = 'gcc',
    DirC = 'cdir'
}